use "some"
use "folder/a"

attr push(god, devil, human)

attr absolute, madness, because, why, not
struct GUA:
  moo: G[O]

attr pop

fun ++[T](a, b: T) -> T: a + b

fun main -> i64:
  var 
    val
    val2 = val
    unknown

  val ++= 10

  return val