use "some"
use "folder/a"

attr push(god, devil, human)

attr absolute, madness, because, why, not
struct GUA:
  moo: G[O]

attr pop

fun ++[T](a: T, b: T) -> T: a + b

fun main -> i64:
  var val
  var val2 = val

  val ++= 10

  return val