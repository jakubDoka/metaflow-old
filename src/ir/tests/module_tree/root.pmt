use "some"
use "folder/a"

attr push(god, devil, human)

attr absolute, madness, because, why, not
struct GUA:
  moo: G[O]

attr pop

fun goo[T](x: T) -> T: x

fun ++[T](a, b: T) -> T: a + b

fun main -> i64:
  var 
    val
    val2 = val

  val ++= 10

  var foo = goo(val)

  foo += if foo == 10: 1 else: 1

  if foo == 10:
    return foo
  else:
    foo += 1
  
  if foo <= 30:
    foo = 10
  else:
    return foo
  
  if foo > 10:
    foo = 20
  else:
    foo = 30 | 6

  return val