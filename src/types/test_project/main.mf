use 
  hello "main/module"

struct Bogus

break

struct Focus:
  a, b, c: Bogus
  priv f: &Focus
  pub embed gro: Gro

struct Gro:
  s, t, u: i32

struct IntOption:
  gua: (Gro, Bogus, (int, int), (Focus, Focus, (int,)))
  o: hello::Option[int]

struct SomeArray:
  a: [int, 8 * 10]

struct Interface:
  value: uint
  add: fun(uint)
  sub: fun(uint)
  mul: fun(uint)
  div: fun(uint)
  get: fun -> uint

struct Cool: 
  g: Goof[Interface]
  f: StackFrame
