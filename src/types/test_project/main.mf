use "main/module"

struct Bogus

struct Focus:
  a, b, c: Bogus
  priv f: &Focus
  pub embed gro: Gro

struct Gro:
  s, t, u: i32

struct IntOption:
  o: module::Option[int]

struct SomeArray:
  a: [int, 8]

struct Interface:
  value: uint
  add: fun(uint)
  sub: fun(uint)
  mul: fun(uint)
  div: fun(uint)
  get: fun -> uint
