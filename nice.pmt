attr linkage(import), call_conv(windows_fastcall)
fun putchar(i: i32)

fun main -> i64:
  var 
    a = "Hello, World!\n"
    b = 0
  let addr = a.data  
  loop:
    putchar(*((a.data as i64 + b) as &u8) as i32)
    b = b + 1
    if b >= a.len as i64:
      break
  return 0
        