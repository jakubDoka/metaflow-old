
attr linkage(import), call_conv(platform)
fun putchar(c: i32)

attr entry
fun main -> i64:
  var
    a = "Hello, World!\n"
    b = a as uint
  loop:
    let char = *(b as &u8) 
    if char == 0u8:
      break
    char.i32().putchar()
    b += 1u

  return 0